# Example 1
add = lambda x, y: x + y

# Example 2
multiply = lambda x, y: x * y

# Example 3
is_even = lambda x: x % 2 == 0

# Example 4
greet = lambda name: f"Привет, {name}!"

# Example 5
get_grade = lambda score: "Great" if score >= 90 else "Good" if score >= 70 else "Satisfactorily"